import 'package:flutter/material.dart';

class AsistenciaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Detalles de las asistencias'),
      ),
    );
  }
}
